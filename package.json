{
  "name": "fakeit",
  "version": "1.0.0",
  "description": "Social impostor word game with real-time multiplayer",
  "main": "backend/index.js",
  "scripts": {
    "dev": "./start-dev.sh",
    "start": "cd backend && node index.js",
    "start:backend": "cd backend && npm start",
    "build": "cd frontend && npm run build",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm run dev",
    "test": "echo \"No tests specified yet\" && exit 0",
    "status": "echo \"Backend: http://localhost:5001/api/health\" && echo \"Frontend: http://localhost:3000\" && curl -s http://localhost:5001/api/health | head -1"
  },
  "keywords": [
    "game",
    "multiplayer",
    "impostor",
    "social",
    "react",
    "express",
    "websockets"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
